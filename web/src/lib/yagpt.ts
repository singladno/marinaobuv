import type { ProductDraft } from '@/types/draft';

import { normalizeTextToDraft, validateDraftWithImages } from './yagpt-api';

// Re-export the main functions
export { normalizeTextToDraft, validateDraftWithImages };
