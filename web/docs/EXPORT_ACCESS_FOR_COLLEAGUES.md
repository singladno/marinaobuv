# Как получить файлы экспорта товаров

Этот документ предназначен для коллег из команды Bitrix, которые нуждаются в регулярных выгрузках товаров.

## 📋 Общая информация

- **Частота выгрузки:** Ежедневно в 02:00 по серверному времени
- **Форматы:** CSV и XML
- **Что экспортируется:** Все новые и обновленные товары с момента последней выгрузки

## 🔗 Способ 1: Прямые ссылки из S3 (самый простой)

После каждой выгрузки файлы автоматически загружаются в облачное хранилище (Yandex Object Storage).

### Как получить ссылки:

**Вариант А: Попросить у администратора**
Администратор может получить ссылки командой:
```bash
npm run export:latest
```

**Вариант Б: Из логов сервера**
Ссылки логируются в файл `web/logs/product-export.log`:
```bash
grep "File uploaded to S3" web/logs/product-export.log | tail -2
```

**Вариант В: По шаблону URL**
Если известен базовый URL CDN, файлы доступны по шаблону:
```
https://your-cdn-domain.com/exports/products-export-YYYY-MM-DD.csv
https://your-cdn-domain.com/exports/products-export-YYYY-MM-DD.xml
```

Где `YYYY-MM-DD` - дата выгрузки (например, `2025-01-15`).

### Примеры ссылок:
```
CSV: https://your-cdn-domain.com/exports/products-export-2025-01-15.csv
XML: https://your-cdn-domain.com/exports/products-export-2025-01-15.xml
```

## 📡 Способ 2: Через API

Если у вас есть доступ к API с токеном администратора:

### Получить список всех экспортов:
```bash
GET https://your-domain.com/api/admin/products/export/list
Authorization: Bearer <your-token>
```

### Скачать конкретный файл:
```bash
GET https://your-domain.com/api/admin/products/export/download/products-export-2025-01-15.csv
Authorization: Bearer <your-token>
```

## 💻 Способ 3: Автоматическая загрузка

### Скрипт для автоматической загрузки (пример на Python):

```python
import requests
from datetime import datetime, timedelta

def get_export_url(date_str, format='csv'):
    """Получить URL файла экспорта"""
    base_url = "https://your-cdn-domain.com/exports"
    filename = f"products-export-{date_str}.{format}"
    return f"{base_url}/{filename}"

def download_latest_export(format='csv'):
    """Скачать последний экспорт"""
    # Получаем вчерашнюю дату (экспорт обычно делается ночью)
    yesterday = (datetime.now() - timedelta(days=1)).strftime('%Y-%m-%d')

    url = get_export_url(yesterday, format)
    response = requests.get(url)

    if response.status_code == 200:
        filename = f"products-export-{yesterday}.{format}"
        with open(filename, 'wb') as f:
            f.write(response.content)
        print(f"✅ Downloaded: {filename}")
        return filename
    else:
        print(f"❌ Failed to download: {response.status_code}")
        return None

# Использование
download_latest_export('csv')
download_latest_export('xml')
```

### Скрипт на Bash:

```bash
#!/bin/bash

# Настройки
CDN_BASE="https://your-cdn-domain.com/exports"
DATE=$(date -d "yesterday" +%Y-%m-%d)  # Вчерашняя дата

# Скачать CSV
curl -o "products-export-${DATE}.csv" \
  "${CDN_BASE}/products-export-${DATE}.csv"

# Скачать XML
curl -o "products-export-${DATE}.xml" \
  "${CDN_BASE}/products-export-${DATE}.xml"
```

## 📧 Способ 4: Настроить уведомления

Попросите администратора настроить автоматическую отправку ссылок:
- По email после каждой выгрузки
- В Telegram канал
- В корпоративный мессенджер

## 🔍 Проверка доступности файла

Перед использованием файла можно проверить его доступность:

```bash
# Проверка через curl
curl -I https://your-cdn-domain.com/exports/products-export-2025-01-15.csv

# Если файл доступен, вернется HTTP 200
# Если файл не найден, вернется HTTP 404
```

## 📊 Структура данных

### CSV формат:
- Кодировка: UTF-8 с BOM (для Excel)
- Разделитель: запятая
- Первая строка: заголовки колонок

### XML формат:
- Кодировка: UTF-8
- Структура: `<products><product>...</product></products>`

Подробное описание полей см. в `PRODUCT_EXPORT.md`

## ❓ Часто задаваемые вопросы

**Q: Как часто обновляются файлы?**
A: Ежедневно в 02:00. Экспортируются только новые/измененные товары.

**Q: Что делать, если файл не найден?**
A: Проверьте дату - файлы создаются ночью, поэтому для сегодняшней даты файла еще нет. Используйте вчерашнюю дату.

**Q: Можно ли получить полный экспорт всех товаров?**
A: Да, попросите администратора выполнить ручной экспорт через API с параметром `onlyNew=false`.

**Q: Файлы хранятся долго?**
A: Локальные файлы удаляются через 7 дней, но в S3 они хранятся дольше (зависит от настройки).

**Q: Нужна ли авторизация для доступа к файлам?**
A: Нет, файлы в S3 публичные и доступны по прямой ссылке без авторизации.

## 📞 Контакты

Если возникли проблемы с доступом к файлам, обратитесь к администратору системы MarinaObuv.
